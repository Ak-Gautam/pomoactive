// content.js

// Example: Change the background color of all paragraphs on the page
const paragraphs = document.querySelectorAll('p');
for (const paragraph of paragraphs) {
    paragraph.style.backgroundColor = 'yellow';
}
